"""Retired presentation compatibility module.

`render_analysis_output(...)` was removed after confirming it had no repo-local callers.
Current presentation paths render through `profile_renderers` / output-profile modules directly.
"""
